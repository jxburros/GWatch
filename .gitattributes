# Keep LF everywhere so gofmt, go mod tidy and the embedded web assets
# are byte-identical on Windows runners and checkouts.
* text=auto eol=lf
*.ps1 text eol=crlf
*.png binary
*.ico binary
*.bmp binary
*.syso binary
*.woff2 binary
*.gwbackup binary
