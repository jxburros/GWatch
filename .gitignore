dist/
data/
*.db
*.db-wal
*.db-shm
*.gwbackup
*.log
# The built binaries, at the repository root only. These are anchored with a
# leading slash: unanchored, "gwatch" also matches any directory of that name,
# which silently hid mcp/internal/gwatch from git.
/GWatch
/gwatch
/gwatch.exe
/gwatch-mcp
/gwatch-mcp.exe
coverage.out
coverage.txt
.claude/
gwatch.key
release.key
# devDependencies for the web test suite (tests/web/) only — the shipped
# binary embeds web/ as-is and never sees node_modules.
node_modules/
